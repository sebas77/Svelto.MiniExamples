namespace Svelto.ECS.Example.Survive.Camera
{
    //if a group is so simple that doesn't need adjectives or states, NamedExclusiveGroup can be used to identify a single group
    public class Camera : NamedExclusiveGroup<Camera> { };
}